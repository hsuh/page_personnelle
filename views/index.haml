- if @profile
  = haml :_about
  = haml :_tracks
  = haml :_logic
  = haml :_contact
  = haml :_breaks

  :javascript
    function scrollNav() {
      $('#main-nav a').click(function(){
        //toggle class
        $('.active').removeClass('active');
        $(this).closest('li').addClass('active');
        //animate
        $('html, body').stop().animate(
          { scrollTop: $( $(this).attr('href') ).offset().top - 182 }, 400
        );
        return false;
      });
      $('.scrollTop a').scrollTop();
    }
    scrollNav();
